type User @entity {
  id: ID! # address
  voteWeight: BigInt!
  gno: BigInt!
  lgno: BigInt!
  deposit: BigInt!
  sgno: BigInt!
  osgno: BigInt!
}

type VaultState @entity {
  id: ID!
  profitAccrued: BigInt!
  treasuryShare: BigInt!
  treasuryAsset: BigInt!
  rate: BigInt!
}
